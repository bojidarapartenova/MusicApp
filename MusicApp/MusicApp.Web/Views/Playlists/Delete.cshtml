@using MusicApp.Web.ViewModels.Playlists
@model DeletePlaylistViewModel
@{
    ViewData["Title"] = "Delete Playlist";
}

<link href="~/css/style.css" rel="stylesheet" />

<div class="row">
    <div class="col-md-4">
        <h1>@ViewData["Title"]</h1>
        <hr />
        <p style="color: white">
            Are you sure you want to delete <strong>@Model.Title</strong>?
        </p>
        <div class="d-flex align-items-center gap-3 mt-4">
            <form asp-action="Delete" method="post" class="m-0">
                <input type="hidden" asp-for="Id" />
                <input type="hidden" asp-for="Title" />
                <button type="submit" class="btn btn-danger" style="border-radius: 6px; padding: 10px 16px;">
                    <i class="fas fa-trash"></i> Confirm Delete
                </button>
            </form>

            <a asp-controller="Playlists"
               asp-action="Index"
               id="btn-cancel">
                <i class="fas fa-times-circle"></i> Cancel
            </a>
        </div>
    </div>
</div>